---
description: Runs MP's /implement skill but doesn't commit and does not run all the tests
argument-hint: "<path-to-spec-or-ticket>"
---
/implement $@, BUT DO NOT run all the tests, run only affected ones. Do not do git commit at the end, I will do it manually.
